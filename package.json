{
  "name": "stable-diffusion-webui",
  "version": "0.0.0",
  "devDependencies": {
    "eslint": "^8.40.0"
  },
  "scripts": {
    "lint": "eslint .",
    "fix": "eslint --fix .",
    "build": "apt update && apt -y install wget git python3 python3-venv"
  }
}
